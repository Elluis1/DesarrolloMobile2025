For the docker containers

cd ./docker
docker compose up --build or docker compose up -d

For the app on android open android studio and start some android emulator

npm run android

with that the app should be working fine